#include <stdio.h>
int req(int a[], int n)
{
    int x = a[0];
    if()
}
int main()
{
    int n;
    sacnf("%d", &n);
    int a[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &a[i]);
    }
    rec(a, n);
    return 0;
}